initiate
    int MaxSpeed <- 0;
end
on recorded(string maker, int speed) do
    output string Maker, bool NewRecord;
    NewRecord <- @MaxSpeed[MaxSpeed] < speed;
    MaxSpeed <- ite(@MaxSpeed[MaxSpeed] < speed, speed, @MaxSpeed[MaxSpeed]);
    Maker <- maker;
    send fast(Maker, NewRecord);
end